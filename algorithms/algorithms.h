#ifndef ALGORITHMS_H
# define ALGORITHMS_H
# include "utils.h"
typedef struct s_steps
{
	char			*step;
	struct s_steps	*next;
}					t_steps;
# include "utils.h"
// void				set_id_with_bubble_sort(t_stack *stack_a);
#endif
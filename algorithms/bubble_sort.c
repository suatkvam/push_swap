#include <utils.h>

void bubble_sort(int arr*,int len)
{
    int i;
    int j;

    i = 0;
    while (i < len -1)
    {
        while (j < len - 1 - i)
        {
            if(arr[j] > arr[j+1])
            {

            }
            j++;
        }
        i++;
    }
    
}